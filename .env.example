# 开发环境通过 Vite 代理访问后端，无需改 CORS
# 复制为 .env.development 后启动：pnpm dev
# 后端默认运行在 http://localhost:3001

# 首页演示兜底：后台故障或数据为空时用 public/assets 演示内容（默认开启）
# 正式上线且数据已就绪时可设为 false
VITE_ENABLE_DEMO_FALLBACK=true

# 仅当前后端分域名部署、且不通过 nginx 反代 /api 与 /uploads 时启用：
# VITE_API_BASE=https://api.example.com
# 同时图片路径需要后端返回绝对 URL，或在前端 nginx 配置 /uploads 反代到后端
