# 开发环境通过 Vite 代理访问后端，无需改 CORS
# 前端固定使用 5174（5173 可能被其他项目占用）
# 后端默认运行在 http://localhost:3001
VITE_API_PORT=3001
VITE_ENABLE_DEMO_FALLBACK=true
VITE_ENABLE_DEMO_FALLBACK=true

# 开发环境 baseURL 留空：走 vite.config.ts 的 proxy，避免跨域
# 生产环境若前后端同域（nginx 反代）也留空；分域名部署才需配置
VITE_API_BASE=